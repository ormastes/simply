use std.web_framework.app.{WebApp}

fn main():
    val app = WebApp.new("app.sdn")?

    app.mount_routes("routes.sdn")?

    print "[TodoApp] Starting at http://localhost:3000"

    app.start()?
    app.wait()
