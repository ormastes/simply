# Tool Experts

Tool experts own process knowledge for external and repo-local tools such as
MCP servers, LSP servers, build tools, package managers, test runners, and
browser automation.

Each tool expert should record:

- command entry points
- setup and authentication steps
- hot-path and startup constraints
- verification commands
- known failure modes and recovery steps
