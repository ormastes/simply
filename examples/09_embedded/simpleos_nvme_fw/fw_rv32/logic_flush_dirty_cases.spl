use logic_flush_dirty_clamp_cases.*
use logic_flush_dirty_valid_cases.*

fn rv32_flush_dirty_cases_selftest() -> i32:
    if rv32_flush_dirty_valid_cases_selftest() != 0:
        return 1
    if rv32_flush_dirty_clamp_cases_selftest() != 0:
        return 1
    0
