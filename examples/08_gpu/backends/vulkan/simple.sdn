project:
  name: svmg_hello_vulkan

# Same program, different backend. Only this file differs between the
# cuda/, vulkan/ and metal/ directories.
gpu:
  backend: vulkan        # auto | cuda | vulkan | metal
  submode: interpreter
  arch: auto
