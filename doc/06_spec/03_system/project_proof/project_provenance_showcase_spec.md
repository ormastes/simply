# Project provenance showcase

Source: `test/03_system/project_proof/project_provenance_showcase_spec.spl`.

The simulator proves that a clean, pinned beta receipt can be rendered as
verified and that changing the same receipt to `tree=dirty` fails closed.
Run the portable evidence test first:

```sh
sh test/project_provenance_showcase_test.sh
```
